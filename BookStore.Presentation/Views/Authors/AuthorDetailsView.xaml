<UserControl x:Class="BookStore.Presentation.Views.Authors.AuthorDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookStore.Presentation.Views.Authors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock 
            Grid.Row="0"
            Text="{Binding SelectedAuthor.FullName}" 
            FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <DataGrid   
            Grid.Row="1"
            ItemsSource="{Binding SelectedAuthor.BooksSortedByRelease}"
            AutoGenerateColumns="False"
            HeadersVisibility="Column"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            IsReadOnly="True"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Titel" Binding="{Binding Title}" Width="200" />
                <DataGridTextColumn Header="Utgivningsår" Binding="{Binding ReleaseDate, StringFormat={}{0:yyyy}}" Width="100"/>
                <DataGridTextColumn Header="Genre" Binding="{Binding Genre.Name}" Width="150"/>
                <DataGridTextColumn Header="ISBN" Binding="{Binding Isbn13}" Width="150" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
