<UserControl x:Class="BookStore.Presentation.Views.Authors.AuthorsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BookStore.Presentation.Views.Authors"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Background="WhiteSmoke" Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel 
            Orientation="Horizontal" 
            Margin="10" 
            Grid.Row="0">
            
            <TextBox 
                Width="300" Height="25"
                Padding="5"
                Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
            <Button 
                Content="Sök"
                Margin="5 0"
                Padding="5 2"
                Command="{Binding SearchCommand}"
                Background="#007ACC"
                Foreground="White"
                BorderThickness="0"
                Cursor="Hand"/>
        </StackPanel>

        <TextBlock 
            Grid.Row="2" 
            Text="Sök på boktitel, författare, eller ISBN och tryck på sök."
            Margin="10"
            Visibility="{Binding IsBeforeSearch, Converter={StaticResource BoolToVisibilityConverter}}" />

        <TextBlock 
            Grid.Row="2" 
            Text="Inga författare hittades."
            Margin="10"
            Visibility="{Binding IsEmptyResult, Converter={StaticResource BoolToVisibilityConverter}}" />


        <TextBlock 
            Grid.Row="1"
            Text="Klicka på en författare för att se en lista på deras böcker." 
            Margin="10" 
            FontStyle="Italic" 
            Foreground="Gray"
            Visibility="{Binding HasResults, Converter={StaticResource BoolToVisibilityConverter}}" />
        
        <DataGrid 
            Grid.Row="2" 
            HorizontalAlignment="Left"
            ItemsSource="{Binding Authors}"
            SelectedItem="{Binding SelectedAuthor}"
            AutoGenerateColumns="False" 
            Margin="10"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            HeadersVisibility="Column"
            IsReadOnly="True"
            Visibility="{Binding HasResults, Converter={StaticResource BoolToVisibilityConverter}}"
            >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Namn" Binding="{Binding FullName}" Width="200" />
                <DataGridTextColumn Header="Född" Binding="{Binding Birth, StringFormat={}{0:yyyy}}" Width="50" />
                <DataGridTextColumn Header="Böcker" Binding="{Binding BookCountDisplay}" Width="50" />
                
            </DataGrid.Columns>
        </DataGrid>

        <local:AuthorDetailsView Grid.Row="3" Visibility="{Binding IsAuthorSelected, Converter={StaticResource BoolToVisibilityConverter}}" />
    </Grid>
</UserControl>
