<UserControl x:Class="BookStore.Presentation.Views.Shell.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookStore.Presentation.Views.Shell"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <StackPanel Background="#F8F8F8" Margin="0" >

        <!-- Böcker -->
        <TextBlock Text="Böcker" FontWeight="Bold" Margin="10 10 0 5"/>
        <Button Content="Sök bok"
                Command="{Binding ShowBooksCommand}"
                Style="{StaticResource NavButtonStyle}" />
        <Button Content="Alla böcker"
                Style="{StaticResource NavButtonStyle}" />

        <!-- Författare -->
        <TextBlock Text="Författare" FontWeight="Bold" Margin="10 15 0 5"/>
        <Button Content="Sök författare" Style="{StaticResource NavButtonStyle}" />

        <!-- Lager -->
        <TextBlock Text="Lager" FontWeight="Bold" Margin="10 15 0 5"/>
        <Button Content="Lagerstatus" Style="{StaticResource NavButtonStyle}" />
        <Button Content="Lågt i lager" Style="{StaticResource NavButtonStyle}" />

        <!-- Beställningar -->
        <TextBlock Text="Beställningar" FontWeight="Bold" Margin="10 15 0 5"/>
        <Button Content="Skapa beställning" Style="{StaticResource NavButtonStyle}" />
        <Button Content="Lista beställningar" Style="{StaticResource NavButtonStyle}" />

        <!-- Administration -->
        <TextBlock Text="Administration" FontWeight="Bold" Margin="10 15 0 5"/>
        <Button Content="Anställda" Style="{StaticResource NavButtonStyle}" />
        <Button Content="Butiker" Style="{StaticResource NavButtonStyle}" />
    </StackPanel>

    <!--<Grid>
        <TreeView>
            <TreeViewItem Header="Böcker">
                <TreeViewItem Header="Sök bok" />
                <TreeViewItem Header="Alla böcker" />
            </TreeViewItem>

            <TreeViewItem Header="Författare" />

            <TreeViewItem Header="Lager">
                <TreeViewItem Header="Lagerstatus" />
                <TreeViewItem Header="Lågt i lager" />
            </TreeViewItem>

            <TreeViewItem Header="Beställningar">
                <TreeViewItem Header="Skapa beställning" />
                <TreeViewItem Header="Lista beställningar" />
            </TreeViewItem>

            <TreeViewItem Header="Administration">
                <TreeViewItem Header="Anställda" />
                <TreeViewItem Header="Butiker" />
            </TreeViewItem>
        </TreeView>    
    </Grid>-->
</UserControl>
