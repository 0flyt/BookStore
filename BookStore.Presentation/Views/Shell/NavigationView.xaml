<UserControl x:Class="BookStore.Presentation.Views.Shell.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookStore.Presentation.Views.Shell"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <StackPanel Margin="10">
        <TextBlock Text="Böcker" FontWeight="Bold" Margin="0 0 0 5"/>
        <Button Content="Sök bok"
                Command="{Binding ShowBooksCommand}"
                Margin="0 0 0 5" />
        <Button Content="Alla böcker" />

        <TextBlock Text="Författare" FontWeight="Bold" Margin="10 10 0 5"/>
        <Button Content="Sök författare" />

        <TextBlock Text="Lager" FontWeight="Bold" Margin="10 10 0 5"/>
        <Button Content="Lagerstatus" />
        <Button Content="Lågt i lager" />
    </StackPanel>

    <!--<Grid>
        <TreeView>
            <TreeViewItem Header="Böcker">
                <TreeViewItem Header="Sök bok" />
                <TreeViewItem Header="Alla böcker" />
            </TreeViewItem>

            <TreeViewItem Header="Författare" />

            <TreeViewItem Header="Lager">
                <TreeViewItem Header="Lagerstatus" />
                <TreeViewItem Header="Lågt i lager" />
            </TreeViewItem>

            <TreeViewItem Header="Beställningar">
                <TreeViewItem Header="Skapa beställning" />
                <TreeViewItem Header="Lista beställningar" />
            </TreeViewItem>

            <TreeViewItem Header="Administration">
                <TreeViewItem Header="Anställda" />
                <TreeViewItem Header="Butiker" />
            </TreeViewItem>
        </TreeView>    
    </Grid>-->
</UserControl>
