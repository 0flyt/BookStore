<UserControl 
    x:Class="BookStore.Presentation.Views.Login.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:BookStore.Presentation.Views.Login"
    mc:Ignorable="d" 
    d:DesignHeight="450" d:DesignWidth="800"
    >
    <Grid Margin="-220 -60 0 0" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        
        <TextBlock 
            Grid.Row="0" 
            Grid.Column="0" 
            Text="Användarnamn: " 
            VerticalAlignment="Center"
            HorizontalAlignment="Right" 
            Margin="10" />
        <ComboBox 
            Grid.Row="0"
            Grid.Column="1"
            VerticalAlignment="Center"
            HorizontalAlignment="Left"
            ItemsSource="{Binding Users}" 
            SelectedItem="{Binding SelectedUser}" 
            DisplayMemberPath="LoginUserName" 
            MinWidth="230"
            Margin="10" />

        <TextBlock 
            Grid.Row="1" 
            Grid.Column="0" 
            Text="Butik: "
            VerticalAlignment="Center"
            HorizontalAlignment="Right"
            Margin="10 "/>
        <ComboBox  
            Grid.Row="1"
            Grid.Column="1"
            HorizontalAlignment="Left"
            ItemsSource="{Binding Stores}" 
            SelectedItem="{Binding SelectedStore}" 
            DisplayMemberPath="CityAndAddress"
            MinWidth="230"
            Margin="10"/>

        <Button 
            Grid.Row="2" 
            Grid.Column="1"
            Content="Login" 
            Command="{Binding LoginCommand}" 
            Width="100"
            HorizontalAlignment="Center"
            Margin="0 20"/>
    </Grid>
</UserControl>
