<UserControl x:Class="BookStore.Presentation.Views.Books.BooksView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BookStore.Presentation.Views.Books"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Background="WhiteSmoke" Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10" Grid.Row="0">
            <TextBox Width="300" Height="25"
                     Padding="5"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Sök"
                    Margin="5 0"
                    Padding="5 2"
                    Command="{Binding SearchCommand}"
                    Background="#007ACC"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"/>
        </StackPanel>

        <TextBlock 
            Grid.Row="2" 
            Text="Sök på boktitel, författare, eller ISBN och tryck på sök."
            Margin="10"
            Visibility="{Binding IsBeforeSearch, Converter={StaticResource BoolToVisibilityConverter}}" />

        <TextBlock 
            Grid.Row="2" 
            Text="Inga böcker hittades."
            Margin="10"
            Visibility="{Binding IsEmptyResult, Converter={StaticResource BoolToVisibilityConverter}}" />


        <TextBlock 
            Grid.Row="1"
            Text="Klicka på en bok för att se detaljer om den." 
            Margin="10,0,10,5" 
            FontStyle="Italic" 
            Foreground="Gray"
            Visibility="{Binding HasResults, Converter={StaticResource BoolToVisibilityConverter}}" />
        
        <DataGrid Grid.Row="2" 
                  HorizontalAlignment="Left"
                  ItemsSource="{Binding Books}"
                  SelectedItem="{Binding SelectedBook}"
                  AutoGenerateColumns="False" 
                  Margin="10"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  HeadersVisibility="Column"
                  IsReadOnly="true"
                  Visibility="{Binding HasResults, Converter={StaticResource BoolToVisibilityConverter}}"
                  >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Titel" Binding="{Binding Title}" Width="200" />
                <DataGridTextColumn Header="Författare" Binding="{Binding AuthorNames}" Width="200" />
                <DataGridTextColumn Header="Genre" Binding="{Binding Genre.Name}" Width="120" />
                <DataGridTextColumn Header="Språk" Binding="{Binding Language}" Width="75" />
                <DataGridTextColumn Header="Format" Binding="{Binding Format.Name}" Width="75" />
                <DataGridTextColumn Header="ISBN" Binding="{Binding Isbn13}" Width="100" />
            </DataGrid.Columns>
        </DataGrid>

        <local:BookDetailsView Grid.Row="3" Visibility="{Binding IsBookSelected, Converter={StaticResource BoolToVisibilityConverter}}" />
    </Grid>
</UserControl>
